@model IEnumerable<Mvc3ViewDemo.Controllers.Blog>
@{
    ViewBag.Title = "StronglyTypedIndex";
}

<h2>StronglyTypedIndex</h2>

<ul>
    @foreach (var item in Model)
    {
        <li><a href="@item.URL">@item.Name</a></li>
    }
</ul>
